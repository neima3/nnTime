# Kairo — Round 6: 10-Phase 10× Program (2026-07-24)

**Goal:** make scheduled push delivery real (the last missing piece of the
reminders story), finish the round-5 iOS leftovers (Focus checklist, Search,
native soundscape), and harden with tests — all verified.

Grounding (verified 2026-07-24):
- Web push works on-demand (subscribe/test), but `/api/v1/jobs/tick` computes
  notification jobs and **never sends** — no scheduled delivery, no cron.
- iOS still missing: Focus session checklist (focus doesn't link an activity),
  Search/quick-jump, native soundscape.
- iOS onboarding has no feature orientation (web has a "superpowers" screen).
- Intentions/quiet-hours logic lives inline (not unit-testable yet).

## Execution model
- Design/native/interactive → me (Opus, main loop).
- Mechanical (tests) → Sonnet subagents, verified before tick.
- Each phase: build/gates green + evidence (screenshot/test) before tick.
- Commit in logical batches; deploy web to Coolify + verify live; iOS on sim.

## Progress tracker
### Reminders — the last piece
- [ ] H1: Scheduled push delivery — jobs/tick sends via sendToUser (quiet-hours
      + dedupe aware); document/enable cron; verify end-to-end

### iOS — finish parity
- [ ] H2: iOS Focus session checklist — link activity, tick steps mid-session
- [ ] H3: iOS Search / quick-jump — find activities across a window + jump
- [ ] H4: iOS native soundscape — AVAudioEngine ambient, wired to Focus rituals
- [ ] H5: iOS onboarding — a closing "superpowers" orientation screen
- [ ] H6: iOS parity polish — routines "manage on web" note, review confetti/dots

### Hardening
- [ ] H7: Extract intentions + quiet-hours logic to a testable lib
- [ ] H8: Unit/integration tests for the extracted logic (subagent)

### Polish + ship
- [ ] H9: Web — reduced-stimulation respect + empty-state sweep
- [ ] H10: Full gates + deploy + live verify (web) + iOS suite green

## Rules
Tokens only; nothing turns red; forgiving copy; browser/simulator evidence per
phase. Web deploy verified on time.neima.me. iOS on simulator. Design/native
never to cheap models. Update tracker + progress.md.
