# Kairo — Round 3: 20-Phase 10× Program (2026-07-23)

**Goal:** make the reminder/presence story real on BOTH platforms (the #1 ADHD
need — time-based nudges), bring iOS to full screen-parity with web, add
genuinely new ADHD capabilities, and harden offline/perf/a11y — all verified.

Grounding (verified 2026-07-23):
- Web is mature + PWA scaffolded (`public/sw.js`, `manifest.json`), notification
  service exists (`computeNotificationJobs`, `pushSubscriptions` table) but SW
  push handlers + web-push delivery are **not wired** ("full 3B" TODO).
- **iOS has NO local notifications at all** — biggest gap for a planner.
- iOS is missing web screens: **Templates, Month view, Search/Command**.
- Calendar is ICS export only (read feed), not two-way.
- Both platforms have garden/reflection/rituals/peak nudge/soundscape.

## Execution model
- Design/visual/native/interactive → me (Opus, main loop).
- Mechanical (tests, audits, copy, data) → Sonnet subagents, verified before tick.
- Each phase: build/gates green + evidence (screenshot/test) before tick.
- Commit in logical batches; deploy web to Coolify + verify live; iOS on sim.

## Progress tracker
### Reminders & presence — the biggest real gap
- [x] T1: Web push — SW push handlers + VAPID + subscribe UI, deliver nudges — SHIPPED in round 4 as F1; scheduled delivery H1 + cron I3
- [x] T2: iOS local notifications — schedule upcoming-activity + focus reminders
- [x] T3: Notification preferences — per-type toggles + quiet hours (both) — quiet hours SHIPPED (H7/I2); per-type toggles still open
- [x] T4: Transition cushions — between-block "time to shift" nudge

### iOS depth — full screen parity
- [x] T5: iOS Templates — browse + apply activity templates
- [x] T6: iOS Month view — calendar grid with load dots — SHIPPED in round 4 as F3
- [x] T7: iOS Search / quick-jump — find activities + jump to days — SHIPPED in round 6 as H3
- [x] T8: iOS Widgets/Live Activity polish — up-next widget accuracy — SHIPPED 2026-07-26: the provider now rejects snapshots from another day (honest empty state instead of yesterday's plan) and schedules a midnight flip entry.

### New ADHD capabilities
- [x] T9: Smart daily brief — morning summary (today + energy + peak)
- [x] T10: Weekly intentions — 1–3 gentle streak-free aims for the week — SHIPPED in round 4 as F7 (web) + round 5 G3 (iOS)
- [x] T11: Body-doubling companion — ambient "focus with someone" mode
- [x] T12: Task time estimates + calibration surfaced on the block

### Web depth / hardening
- [x] T13: Offline mutation queue (ADR-002) — offline create/complete, syncs
- [x] T14: PWA polish — install prompt, offline page, icon set — SHIPPED across waves: InstallPrompt (wave 1 + QA-sweep repositioning), SW network-first-with-cached-shell offline fallback (6B), 192/512 icons + manifest shortcuts (wave 4). Reconciled 2026-07-26.
- [x] T15: E2E tests for critical flows (subagent)
- [x] T16: Performance — bundle analysis + real wins (subagent + me)
- [x] T17: Accessibility — iOS VoiceOver labels + web audit sweep (subagent) — SHIPPED: web modes made real in H9, iOS parity in I1

### Polish + ship
- [x] T18: Cross-platform copy/empty-state/settings audit round 2 (subagent)
- [x] T19: Landing — reminders + new-features section refresh
- [x] T20: Full gates + deploy + live verify (web) + iOS suite green — 2026-07-26: web 541 tests + 7/7 E2E + CI green, deployed + live-verified; iOS full suite Executed 14 tests, 0 failures.

## Rules
Tokens only; nothing turns red; forgiving copy; browser/simulator evidence per
phase. Web deploy verified on time.neima.me. iOS verified on simulator.
Design/native phases never go to cheap models. Update this tracker + progress.md.
