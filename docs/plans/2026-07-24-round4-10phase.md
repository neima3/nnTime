# Kairo — Round 4: 10-Phase 10× Program (2026-07-24)

**Goal:** finish the reminders story (web push, to pair with the iOS local
notifications already shipped), close the last iOS screen-parity gaps (Month,
Search, week edit, settings), add one new ADHD capability, and verify — now
that disk space is freed and browser verification works again.

Grounding (verified 2026-07-24):
- Disk freed to ~26 GB; agent-browser works again (full visual verification back).
- iOS shipped: notifications, Templates, mood, focus break/overtime, Day Rituals,
  garden/reflection/rituals/peak nudge. **Still missing on iOS:** Month, Search,
  week tap-to-edit + nav, settings hour-cycle/week-start.
- Web push is scaffolded (SW push handler, pushSubscriptions table,
  computeNotificationJobs) but **not wired**: no web-push lib, no VAPID, no
  subscribe endpoint/UI, no delivery. This is the biggest remaining reminders gap.
- Focus rituals name a "vibe" on web (soundscape) but iOS Focus has no soundscape.

## Execution model
- Design/native/interactive → me (Opus, main loop).
- Mechanical (tests, audits, data) → Sonnet subagents, verified before tick.
- Each phase: build/gates green + evidence (screenshot/test) before tick.
- Commit in logical batches; deploy web to Coolify + verify live; iOS on sim.

## Progress tracker
### Reminders — finish the story
- [x] F1: Web push — VAPID + subscribe/unsubscribe endpoints + SW handlers +
      Settings opt-in UI + deliver a test nudge
- [ ] F2: Notification preferences — quiet hours + per-type toggles (web + iOS)

### iOS — last screen-parity gaps
- [x] F3: iOS Month view — calendar grid + completion dots + tap to jump
- [ ] F4: iOS Search / quick-jump — find activities across days + jump
- [ ] F5: iOS Week — tap-to-edit blocks + prev/next week nav
- [x] F6: iOS Settings — hour cycle (12/24h) + week start (Sun/Mon), persisted

### New capability + hardening
- [x] F7: Weekly Intentions — 1–3 gentle streak-free aims for the week (web + iOS)
- [ ] F8: iOS Focus soundscape — native ambient generator (parity with web R5)
- [ ] F9: E2E/integration tests for new surfaces (subagent)

### Ship
- [ ] F10: Full gates + deploy + live verify (web) + iOS suite green

## Rules
Tokens only; nothing turns red; forgiving copy; browser/simulator evidence per
phase. Web deploy verified on time.neima.me. iOS on simulator. Design/native
never to cheap models. Update tracker + progress.md.
