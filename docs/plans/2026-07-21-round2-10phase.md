# Kairo — Round 2: 10-Phase 10× Program (2026-07-21)

**Goal:** close the cross-platform gap the last program opened (bring the new
web ADHD features to iOS), make the soundscape story real (procedural audio,
not 12-byte placeholders), pay down the a11y/copy debts flagged in the P9/P19
audits, and add one genuinely new capability — all verified with evidence.

Grounding (verified 2026-07-21):
- Web is mature: today/inbox/week/focus/play/routines/stats/review/templates/
  planner + garden, weekly reflection, focus rituals, peak nudge, energy-aware
  plan, resumable onboarding, landing showcase.
- iOS has Auth/Today/Week/Inbox/Focus/More/Onboarding/Stats/Pick/Play/Review/
  Routines + widgets + Live Activity. **Missing on iOS:** Reward Garden, Weekly
  Reflection, Focus rituals, Peak-focus nudge.
- `public/audio/*.ogg` are 12-byte placeholders → AmbientSounds is a no-op.
- Focus rituals name a "vibe" but don't start any sound.
- Modal focus-trap exists only in CommandPalette (P9 leftover).

## Execution model
- Design/visual/interactive phases → me (Opus, main loop).
- Mechanical (tests, copy sweep, ARIA) → Sonnet subagents, verified before tick.
- Each phase: build/gates green + evidence (screenshot or test) before tick.
- Commit in logical batches; deploy web to Coolify + verify live; iOS on sim.

## Progress tracker
### iOS parity — bring the new web wins native
- [x] R1: iOS Reward Garden on Stats (growth that isn't a streak)
- [x] R2: iOS Weekly Reflection on Stats (gentle patterns digest)
- [x] R3: iOS Focus rituals (named presets on the Focus setup)
- [x] R4: iOS Peak-focus nudge on Today (uses stats focus-hours)

### Depth — make half-built things real
- [x] R5: Procedural Web Audio soundscape engine (replace placeholder .ogg;
      generate rain/forest/ocean/cafe/white-noise) + wire Focus rituals to vibe
- [x] R9: "Protect your peak" — one-tap recurring focus anchor at your peak hour
      (extends the peak nudge; web)

### Debts + hardening (subagent-friendly)
- [x] R6: Web a11y — modal focus-trap across overlays + ARIA combobox on
      Command Palette (P9 leftovers)
- [x] R7: Copy consistency sweep — standardize "Could not X." → warm/contraction
      form across ~15 files (P19 should-fix leftovers)
- [x] R8: Test coverage — pure-helper unit tests for garden/reflection/peak/
      energy derivations

### Ship
- [x] R10: Full web gates + deploy + live verify + full iOS suite green

## Rules
Tokens only; nothing turns red; forgiving copy; browser/simulator evidence per
phase. Web deploy verified on time.neima.me. iOS verified on simulator.
Design phases never go to cheap models.
